# Kickoff prompt for the iOS build session

Paste everything below the line into a fresh Claude Code session started in `~/Projects/murmur`.

---

Context: this repo is Murmur, my cross-platform push-to-talk dictation app. The desktop app (Electron, Windows + macOS) is built, live-verified, and in daily use; do not regress it. We are now building the iOS app and custom keyboard in this same repo. `prd-ios.json` is the spec: execute it Ralph-style, story by story.

Process, in order:

1. Read `CLAUDE.md` (the iOS section holds the hard laws), `prd-ios.json`, and `ROADMAP.md`. Then skim the desktop pipeline you are porting: `src/main/transcribe.js` (prompt rules, chat guard, silence filtering), `src/main/corrections.js`, `src/main/expansions.js`, and `src/main/settings.js` (defaults and semantics). US-101 extracts those rules into `shared/format-spec.json` and `shared/test-vectors.json` before any Swift is written.
2. Work stories in priority order. Every change maps to a story; `passes` flips only after every acceptance criterion is verified, with the verification recorded honestly in `notes`. `xcodebuild test` on the iOS Simulator is the smoke equivalent and must stay green; `npm run smoke` at the repo root must also stay green whenever you touch `shared/` or `src/`. Run `npm run roadmap` after PRD changes. Commit story by story, and `git pull --rebase` before every commit: parallel sessions run on my other machines.
3. Hard constraints, non-negotiable: the keyboard extension never touches the microphone (Apple forbids it; all audio lives in the containing app via the murmur:// bounce, the Wispr Flow shape); the API key lives in the Keychain only; transcripts stay on device; the formatter fails open; no binary assets committed (icons generated by script); the keyboard ships no networking or audio code and stays lean; night studio design system with amber reserved for live states.
4. Anything that needs my physical iPhone, my Apple ID, signing certificates, or App Store Connect is mine to do: stop and tell me exactly what to click. Ask me for my Apple Developer Team ID when signing setup needs it. Document TestFlight steps, never guess them.

Start with US-101 and show me the shared spec extraction before moving to Xcode scaffolding.
